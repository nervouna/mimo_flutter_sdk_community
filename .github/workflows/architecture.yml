name: Architecture Check

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  check-imports:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dart-lang/setup-dart@v1
      - run: dart tool/check_imports.dart
